{
  "name": "fiberguard-session",
  "version": "0.1.0",
  "private": true,
  "description": "Scoped, revocable, spend-limited payment sessions for Fiber apps",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "mock": "node packages/fiber-mock/dist/cli.js start --port 8227",
    "gateway": "node packages/gateway/dist/cli.js start --policy examples/fiberguard.yml --upstream http://127.0.0.1:8227 --port 8787 --approval-ui apps/approval-ui/out",
    "demo": "bash scripts/demo.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.17.0"
}
